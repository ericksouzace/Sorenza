{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal permissions for the Sorenza main window.",
  "windows": ["main"],
  "permissions": ["core:default", "window-state:default", "opener:default"]
}
